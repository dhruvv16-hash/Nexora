[project]
name = "nexora"
version = "0.1.0"
description = "Nexora Finance Dashboard"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0.0",
    "yfinance>=0.2.35",
    "requests>=2.28.0",
    "gunicorn>=21.2.0"
]

[tool.vercel]
entrypoint = "dashboard:app"
